# Environment Variables for Backend

# Claude API Key (Required for AI processing)
# Get your API key from: https://console.anthropic.com/settings/keys
ANTHROPIC_API_KEY=your_claude_api_key_here

# JWT Secret (Optional for MVP, required for production)
# JWT_SECRET=your_jwt_secret_key_here

# Database URL (Optional for MVP, for future user management)
# DATABASE_URL=postgresql://user:password@localhost/litaylored

# CORS Origins (Comma-separated list)
CORS_ORIGINS=http://localhost:3000

# Application Settings
DEBUG=True
